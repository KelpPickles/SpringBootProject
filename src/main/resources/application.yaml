spring:
  datasource:
    url: jdbc:sqlite:knutcollab.db
    driver-class-name: org.sqlite.JDBC

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect

    show-sql: true

jwt:
  secret: knutcollab-secret-key-super-super-long-kakao-plz